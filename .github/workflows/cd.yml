name: CD

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deployment-not-configured:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment is intentionally gated until the hosting target is approved."
